{
  "name": "pos-system-v3",
  "version": "1.0.0",
  "description": "POS System V3 - Point of Sale Application",
  "main": "backend/app.py",
  "scripts": {
    "start": "cd backend && python app.py",
    "build": "cd frontend && npm install && npm run build",
    "heroku-postbuild": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": "18.x",
    "python": "3.11.x"
  }
}
