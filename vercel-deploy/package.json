{
  "name": "pos-system-v3-frontend",
  "version": "1.0.0",
  "scripts": {
    "dev": "next dev",
    "build": "npm run build:react",
    "build:react": "cd ../frontend && npm install && npm run build && cp -r build/* ../vercel-deploy/",
    "start": "next start"
  },
  "dependencies": {},
  "devDependencies": {}
}
